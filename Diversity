#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define tc int t; cin>>t; while (t--)
#define en '\n'
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    string s;
    set<char> st;
    cin>>s;
    int k;
    cin>>k;
    if(s.size()<k)
        cout<<"impossible"<<en;
    else
    {
        for(int i=0;i<s.size();i++)
            st.insert(s[i]);
        if(st.size()<=k)
            cout<<(k-st.size());
        else
            cout<<(0)<<en;
 
    }
 
 
 
 
}
